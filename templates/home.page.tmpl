{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>Home Page</h1>
                <p>Adding new text!!</p>
                <p>Adding new text!!</p>
            </div>
        </div>
    </div>
{{end}}